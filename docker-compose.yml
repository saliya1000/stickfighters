services:
  stick-rumble:
    build: .
    container_name: stick-rumble
    restart: always
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
      # Add other environment variables here or use a .env file
    env_file:
      - .env
